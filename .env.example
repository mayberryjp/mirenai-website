# Leave empty to route API calls through the /api reverse proxy
# (dev: Vite proxy in vite.config.ts, prod: nginx in nginx.conf).
# Set an absolute origin only to bypass the proxy and call mirenai directly.
VITE_API_BASE_URL=
VITE_APP_NAME=mirenai
VITE_APP_ENV=development
